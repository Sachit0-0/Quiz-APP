import React, {useContext} from 'react';
import { QuizContext } from '../Helpers/Contexts';
import { Questions } from '../Helpers/QuestionsBank';


function EndScreen() {
  return (
    <div>EndScreen Component</div>
  );
}

export default EndScreen; // Default export
